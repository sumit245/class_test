function calculateArea ( radius )
{
    const circleArea = Math.PI * radius * radius;
    const squareSide = Math.sqrt( 2 ) * radius;
    const squareArea = squareSide * squareSide;

    const rhombusd1 = 2 * radius;
    const rhombusd2 = 2 * radius;
    const rhombusArea = (rhombusd1 * rhombusd2) /2 ;
}